<view class="page">

<view class="page__bd">

<view wx:for="{{subjects}}" wx:key="{{item.id}}" >
<view class="weui-cells__title">{{item.name}} ¥ {{item.balance}} </view>
<view  class="weui-cells weui-cells_after-title">
  <view wx:for="{{item.capitalAccountsById}}" wx:key="{{item.id}}" data-id='{{item.id}}'  class="weui-cell weui-cell_access" hover-class="weui-cell_active" bindtap='jumpToRecord'>
                <view class="weui-cell__bd">{{item.name}}</view>
                <view class="weui-cell__ft weui-cell__ft_in-access">{{item.balance}}</view>
  </view>
</view>
</view>
</view>
</view>
